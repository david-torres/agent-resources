{{#if notes.length}}
<ul>
  {{#each notes}}
  <li>
    {{{powerRatings this.text}}}
    {{#if this.children.length}}
    <ul>
      {{#each this.children}}<li>{{{powerRatings this.text}}}</li>{{/each}}
    </ul>
    {{/if}}
  </li>
  {{/each}}
</ul>
{{/if}}
