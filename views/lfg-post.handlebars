<div class="container">
  <div class="box lfg-post">
    <h3 class="title is-4">{{post.title}}</h3>
    <p>Date: {{date_tz post.date "dddd, MMMM Do YYYY, h:mm" profile.timezone}}</p>
    <p class="is-size-7">Created by: {{post.creator_name}}</p>
    <p class="mt-4">
      {{#if post.host_id}}
      Conduit: {{post.host_name}}{{else}}<span class="tag is-warning">Conduit needed</span>
      {{/if}}
    </p>
    <div class="box content mt-4">
      {{{markdown post.description}}}
    </div>
    <div class="buttons is-right">
      {{#if (eq post.host_id profile.id)}}
      <button class="button is-danger" hx-delete="/lfg/{{post.id}}/join" hx-confirm="Are you sure?" hx-swap="outerHTML">Unjoin as Conduit</button>
      {{else}}
      {{#inArray (pluck post.join_requests "profile_id") profile.id}}
      <button class="button is-danger" hx-delete="/lfg/{{post.id}}/join" hx-confirm="Are you sure?" hx-swap="outerHTML">Unjoin as Player</button>
      {{else}}
      <button class="button is-primary" hx-get="/lfg/{{post.id}}/join" hx-swap="outerHTML">Join</button>
      {{/inArray}}
      {{/if}}
    </div>
  </div>
</div>